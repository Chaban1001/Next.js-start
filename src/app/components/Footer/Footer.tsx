import { FC } from 'react';
import styles from '../../styles/footer.module.scss';

export const Footer: FC = () => {
  return (
    <footer className={styles.footer__container}>Footer 2024 &copy;</footer>
  );
};
